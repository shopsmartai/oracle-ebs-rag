# Oracle DB — local dev only, never use these in prod
ORA_USER=ragapp
ORA_PASSWORD=ragapp_dev_pwd
ORA_DSN=oracle23ai.orb.local:1521/FREEPDB1

# MCP server's read-only Oracle user (used later)
ORA_RO_USER=mcp_ro
ORA_RO_PASSWORD=mcp_ro_dev_pwd

# Embeddings (free tier works for this project)
COHERE_API_KEY=

# LLM (you'll add your key)
ANTHROPIC_API_KEY=

# Optional: model overrides
ANTHROPIC_MODEL=claude-sonnet-4-6
COHERE_EMBED_MODEL=embed-english-v3.0
